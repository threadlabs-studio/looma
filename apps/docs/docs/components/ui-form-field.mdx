import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Form Field

Form-field composition primitive that links label/control/description semantics.

## Component Reference

<ComponentExamplesApiTabs component="ui-form-field" />

## Attributes

| Name | Type | Default |
| --- | --- | --- |
| `invalid` | `boolean` | `false` |
| `required` | `boolean` | `false` |
| `disabled` | `boolean` | `false` |

## Properties

| Name | Type |
| --- | --- |
| `invalid` | `boolean` |
| `required` | `boolean` |
| `disabled` | `boolean` |

## Events

No custom events.

## Slots

Default slot; recommended children:

- `label`
- control (`ui-input`, `input`, `textarea`, `select`, `button`)
- optional help/error text with `data-slot="help"` and `data-slot="error"`

## SSR Markup

```html
<ui-form-field required>
  <label for="email">Email</label>
  <ui-input>
    <input id="email" name="email" type="email" />
  </ui-input>
  <small data-slot="help">We only use this for account notifications.</small>
</ui-form-field>
```

## Framework Snippets

<Tabs groupId="framework" defaultValue="vue">
  <TabItem value="vue" label="Vue">

```vue
<script setup lang="ts">
import { FormField, Input } from "@threadlabs/looma-vue";
</script>

<template>
  <FormField required>
    <label for="email">Email</label>
    <Input>
      <input id="email" name="email" type="email" />
    </Input>
    <small data-slot="help">We only use this for account notifications.</small>
  </FormField>
</template>
```

  </TabItem>
  <TabItem value="svelte" label="Svelte">

```svelte
<ui-form-field required>
  <label for="email">Email</label>
  <ui-input>
    <input id="email" name="email" type="email" />
  </ui-input>
  <small data-slot="help">We only use this for account notifications.</small>
</ui-form-field>
```

  </TabItem>
  <TabItem value="react" label="React">

```tsx
import { FormField, Input } from "@threadlabs/looma-react";

export function Example() {
  return (
    <FormField required>
      <label htmlFor="email">Email</label>
      <Input>
        <input id="email" name="email" type="email" />
      </Input>
      <small data-slot="help">We only use this for account notifications.</small>
    </FormField>
  );
}
```

  </TabItem>
</Tabs>
