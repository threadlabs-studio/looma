import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# ui-inline

Inline layout primitive for row-style content with wrapping support.

## Component Reference

<ComponentExamplesApiTabs component="ui-inline" />

## Attributes

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `gap` | `xs \| s \| m \| string` | `s` | Spacing between items. |
| `align` | `start \| center \| end \| stretch` | `center` | Cross-axis alignment. |
| `justify` | `start \| center \| end \| between` | `start` | Main-axis distribution. |
| `wrap` | `wrap \| nowrap` | `wrap` | Row wrapping behavior. |

## Properties

| Name | Type |
| --- | --- |
| `gap` | `string` |
| `align` | `string` |
| `justify` | `string` |
| `wrap` | `string` |

## Events

No custom events.

## Slots

Default slot only (natural children).

## SSR Markup

```html
<ui-inline gap="s" wrap="wrap">
  <button type="button">Edit</button>
  <button type="button">Share</button>
  <button type="button">Archive</button>
</ui-inline>
```

## Framework Snippets

<Tabs>
  <TabItem value="react" label="React">

```tsx
import { Inline } from "@looma/react";

export function Example() {
  return <Inline gap="s" wrap="wrap">...</Inline>;
}
```

  </TabItem>
  <TabItem value="vue" label="Vue">

```ts
import { h } from "vue";
import { Inline } from "@looma/vue";

export const Example = { setup: () => () => h(Inline, { gap: "s", wrap: "wrap" }) };
```

  </TabItem>
  <TabItem value="svelte" label="Svelte">

```svelte
<ui-inline gap="s" wrap="wrap">
  <button type="button">Edit</button>
  <button type="button">Share</button>
</ui-inline>
```

  </TabItem>
</Tabs>
