import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Disclosure

Progressively enhances a button/content pair into an accessible disclosure.

## Component Reference

<ComponentExamplesApiTabs component="ui-disclosure" />

## Attributes

| Name | Type | Default |
| --- | --- | --- |
| `open` | `boolean` | `false` |
| `default-open` | `boolean` | `false` |
| `disabled` | `boolean` | `false` |

## Properties

| Name | Type |
| --- | --- |
| `open` | `boolean` |
| `defaultOpen` | `boolean` |
| `disabled` | `boolean` |

## Events

| Name | Detail |
| --- | --- |
| `open` | `{ open: true, reason, trigger }` |
| `close` | `{ open: false, reason, trigger }` |

## Slots

Default slot with authored trigger/content children.

## SSR Markup

```html
<ui-disclosure>
  <button type="button" aria-controls="faq-a1">What is light DOM?</button>
  <div id="faq-a1" hidden>Inspectable SSR markup with progressive behavior.</div>
</ui-disclosure>
```

## Framework Snippets

<Tabs groupId="framework" defaultValue="vue">
  <TabItem value="vue" label="Vue">

```vue
<script setup lang="ts">
import { Disclosure } from "@looma/vue";

function onOpen(detail: unknown) {
  console.log(detail);
}
</script>

<template>
  <Disclosure @open="onOpen">
    <button type="button" aria-controls="faq-a1">What is light DOM?</button>
    <div id="faq-a1" hidden>Inspectable SSR markup with progressive behavior.</div>
  </Disclosure>
</template>
```

  </TabItem>
  <TabItem value="svelte" label="Svelte">

```svelte
<script lang="ts">
  import { bindAdapter } from "@looma/svelte";
  const options = { onOpen: (detail: unknown) => console.log(detail) };
</script>

<ui-disclosure use:bindAdapter={options}>
  <button type="button" aria-controls="faq-a1">What is light DOM?</button>
  <div id="faq-a1" hidden>Inspectable SSR markup with progressive behavior.</div>
</ui-disclosure>
```

  </TabItem>
  <TabItem value="react" label="React">

```tsx
import { Disclosure } from "@looma/react";

export function Example() {
  return (
    <Disclosure onOpen={(detail) => console.log(detail)}>
      <button type="button" aria-controls="faq-a1">What is light DOM?</button>
      <div id="faq-a1" hidden>Inspectable SSR markup with progressive behavior.</div>
    </Disclosure>
  );
}
```

  </TabItem>
</Tabs>
