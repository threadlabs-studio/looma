import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

<div className="looma-component-header">

<p className="component-tag">&lt;ui-button&gt;</p>

# Button

Essential button primitive that enhances semantic button markup and styling hooks.

**Import:** `import "@threadlabs/looma"` — or use `Button` from `@threadlabs/looma/vue`.

</div>

## Examples & API

<ComponentExamplesApiTabs
  component="ui-button"
  preview={
    <>
      <ui-button variant="solid" size="sm">
        <button type="button">Save</button>
      </ui-button>
      <ui-button variant="outline" size="sm">
        <button type="button">Cancel</button>
      </ui-button>
      <ui-button variant="ghost" size="sm">
        <button type="button">Ghost</button>
      </ui-button>
    </>
  }
/>

## SSR Markup

```html
<ui-button variant="solid" size="sm">
  <button type="button" aria-label="Save page">
    <span aria-hidden="true">✦</span>
    Save page
  </button>
</ui-button>
```

## Framework Snippets

```vue
<script setup lang="ts">
import { Button } from "@threadlabs/looma/vue";
</script>

<template>
  <Button variant="solid" size="sm">
    <button type="button" aria-label="Save page">
      <span aria-hidden="true">✦</span>
      Save page
    </button>
  </Button>
</template>
```
