import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# ui-dialog

Overlay primitive that coordinates native `dialog` behavior with shared overlay stack rules.

## Attributes

| Name | Type | Default | Notes |
| --- | --- | --- | --- |
| `open` | `boolean` | `false` | Controls open state. |
| `default-open` | `boolean` | `false` | Initial uncontrolled open state. |
| `modal` | `boolean-like` | `true` | `modal="false"` disables modal mode. |

## Properties

| Name | Type |
| --- | --- |
| `open` | `boolean` |
| `defaultOpen` | `boolean` |
| `modal` | `boolean` |

## Events

| Name | Detail |
| --- | --- |
| `open` | `{ open: true, reason, trigger }` |
| `close` | `{ open: false, reason, trigger }` |

## Slots

Default slot (typically contains one native `<dialog>` child).

## SSR Markup

```html
<ui-dialog open>
  <dialog>
    <h2>Settings</h2>
    <p>Dialog body copy.</p>
    <button value="close">Close</button>
  </dialog>
</ui-dialog>
```

## Framework Snippets

<Tabs>
  <TabItem value="react" label="React">

```tsx
import { Dialog } from "@ui/react";

export function Example() {
  return <Dialog onClose={(detail) => console.log(detail)}>...</Dialog>;
}
```

  </TabItem>
  <TabItem value="vue" label="Vue">

```ts
import { h } from "vue";
import { Dialog } from "@ui/vue";

export const Example = { setup: () => () => h(Dialog, { onClose: (detail: unknown) => console.log(detail) }) };
```

  </TabItem>
  <TabItem value="svelte" label="Svelte">

```svelte
<script lang="ts">
  import { bindAdapter } from "@ui/svelte";
  const options = { onClose: (detail: unknown) => console.log(detail) };
</script>

<ui-dialog use:bindAdapter={options}>...</ui-dialog>
```

  </TabItem>
</Tabs>
