---
slug: /components/ui-cover
---

import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Cover

A layout primitive that centres its content both ways, with room around it: the frame for a view that is one card or one message, such as sign-in. `size="screen"` (the default) is at least the viewport's height; `size="fill"` takes the space its parent gives it.

<ComponentExamplesApiTabs component="ui-cover" />
