import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Tabs

Tablist primitive that synchronizes tab focus, selection, and panel visibility.

## Component Reference

<ComponentExamplesApiTabs component="ui-tabs" />

## Attributes

| Name | Type | Default |
| --- | --- | --- |
| `value` | `string` | first tab id |
| `default-value` | `string` | empty |
| `orientation` | `horizontal \| vertical` | `horizontal` |

## Properties

| Name | Type |
| --- | --- |
| `value` | `string` |
| `defaultValue` | `string` |
| `orientation` | `"horizontal" \| "vertical"` |

## Events

| Name | Detail |
| --- | --- |
| `select` | `{ value, previousValue?, trigger }` |

## Slots

Default slot with authored tablist/tabs/panels.

## SSR Markup

```html
<ui-tabs>
  <div role="tablist" aria-label="Sections">
    <button role="tab" id="tab-overview" aria-controls="panel-overview">Overview</button>
    <button role="tab" id="tab-details" aria-controls="panel-details">Details</button>
  </div>
  <section role="tabpanel" id="panel-overview" aria-labelledby="tab-overview">Overview content</section>
  <section role="tabpanel" id="panel-details" aria-labelledby="tab-details" hidden>Details content</section>
</ui-tabs>
```

## Framework Snippets

<Tabs groupId="framework" defaultValue="vue">
  <TabItem value="vue" label="Vue">

```vue
<script setup lang="ts">
import { Tabs as UITabs } from "@looma/vue";

function onSelect(detail: unknown) {
  console.log(detail);
}
</script>

<template>
  <UITabs @select="onSelect">
    <div role="tablist" aria-label="Sections">
      <button role="tab" id="tab-overview" aria-controls="panel-overview">Overview</button>
      <button role="tab" id="tab-details" aria-controls="panel-details">Details</button>
    </div>
    <section role="tabpanel" id="panel-overview" aria-labelledby="tab-overview">Overview content</section>
    <section role="tabpanel" id="panel-details" aria-labelledby="tab-details" hidden>Details content</section>
  </UITabs>
</template>
```

  </TabItem>
  <TabItem value="svelte" label="Svelte">

```svelte
<script lang="ts">
  import { bindAdapter } from "@looma/svelte";
  const options = { onSelect: (detail: unknown) => console.log(detail) };
</script>

<ui-tabs use:bindAdapter={options}>...</ui-tabs>
```

  </TabItem>
  <TabItem value="react" label="React">

```tsx
import { Tabs as UITabs } from "@looma/react";

export function Example() {
  return <UITabs onSelect={(detail) => console.log(detail)}>...</UITabs>;
}
```

  </TabItem>
</Tabs>
