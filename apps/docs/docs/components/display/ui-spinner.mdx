---
slug: /components/ui-spinner
---

import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Spinner

Something is on its way. With a `label` it is a status that assistive technology announces; beside visible text that already says so, leave the label off. It holds still under reduced motion.

<ComponentExamplesApiTabs component="ui-spinner" />
