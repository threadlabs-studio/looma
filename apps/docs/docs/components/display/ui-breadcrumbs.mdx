---
slug: /components/ui-breadcrumbs
---

import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Breadcrumbs

Where a view sits: a trail of `ui-breadcrumb-item` from the top down to the current view, in a navigation landmark. Every step but the current one holds a link; a chevron leads each step after the first. It wraps rather than scrolls, so every step stays reachable.

<ComponentExamplesApiTabs component="ui-breadcrumbs" />
