---
slug: /components/ui-description-list
---

import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Description List

Named values: each `ui-description-item` is a `term` and its value. `layout="rows"` lines terms up in one column and values in the next, for a short summary; `layout="tiles"` sets each pair on its own tile, term above value, for figures to scan.

<ComponentExamplesApiTabs component="ui-description-list" />
