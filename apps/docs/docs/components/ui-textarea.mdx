import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Textarea

Multiline text-input primitive that mirrors value, rows, disabled, read-only, and invalid state to a native `<textarea>`.


<ComponentExamplesApiTabs component="ui-textarea" />
