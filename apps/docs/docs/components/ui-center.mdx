import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Center

Container primitive for readable measure and horizontal gutters.

## Component Reference

<ComponentExamplesApiTabs component="ui-center" />

## Attributes

| Name | Type | Default |
| --- | --- | --- |
| `measure` | `narrow \| wide \| string` | `65ch` |
| `gutters` | `s \| m \| l \| string` | `m` |

## Properties

| Name | Type |
| --- | --- |
| `measure` | `string` |
| `gutters` | `string` |

## Events

No custom events.

## Slots

Default slot only.

## SSR Markup

```html
<ui-center measure="wide" gutters="m">
  <h1>Page Title</h1>
  <p>Readable line length with consistent side gutters.</p>
</ui-center>
```

## Framework Snippets

```vue
<script setup lang="ts">
import { Center } from "@threadlabs/looma/vue";
</script>

<template>
  <Center measure="wide" gutters="m">
    <h1>Page Title</h1>
    <p>Readable line length with consistent side gutters.</p>
  </Center>
</template>
```
