---
slug: /components/ui-breadcrumb-item
---

import { ComponentExamplesApiTabs } from "@site/src/components/ComponentExamplesApiTabs";

# Breadcrumb Item

One step in a Breadcrumbs trail: a link up to that place, or, with `current`, the view you are on (`aria-current="page"`). An icon slotted in the link sits with its name. See [Breadcrumbs](/components/ui-breadcrumbs).

<ComponentExamplesApiTabs component="ui-breadcrumb-item" />
